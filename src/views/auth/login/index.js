import React from 'react';
import Login from './components/login';
import {LoginContainer} from './elements';

const Auth = () => {
  return (
    <LoginContainer>
      <Login />
    </LoginContainer>
  );
};

export default Auth;
